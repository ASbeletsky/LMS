@model IEnumerable<UsersViewModel>
@{
    ViewData["Title"] = "List";
}

<p>
    <a asp-action="Register">Register new user</a>
</p>

@if (Model.Any())
{
    <div class="table-responsive">
        <table class="table table-bordered">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.user.UserName)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.user.Name)
                    </th>
                    <th>
                        Role
                    </th>
                    <th>
                        Actions
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            <label>@item.user.UserName</label>
                        </td>
                        <td>
                            <label>@item.user.Name</label>
                        </td>
                        <td>
                            <label>@item.role</label>
                        </td>
                        <td>
                            <a asp-action="Edit" asp-route-id="@item.user.Id">Edit</a>
                            <a href="#" data-toggle="modal" data-target="#deleteUser" data-arg-id="@item.user.Id">Delete</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
}
else
{
    <center>No users found</center>
}

@Html.Partial("Dialog", new DialogViewModel("deleteUser")
{
    Content = "Are you sure that you want to delete this user?",
    SubmitAction = "Delete",
    SubmitController = "Account"
})